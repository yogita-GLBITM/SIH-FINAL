import {useState} from 'react';import Logo from '../common/Logo';import './navbar.css';
export default function Navbar({user}){const [open,setOpen]=useState(false);const initials=(user?.name||'AV').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase();return <header className="navbar"><div className="navbar-inner"><a className="nav-brand" href="#hero"><Logo size={34}/>AVYSURE</a><nav><a href="#itinerary">Itinerary</a><a href="#weather">Weather</a><a href="#expense">Expenses</a><a href="#hidden-gems">Hidden Gems</a><a href="#geofence">GeoFence</a><a href="#sos">SOS</a></nav><div className="profile"><button className="profile-btn" onClick={()=>setOpen(!open)}><i>{initials}</i><span><b>{user?.name||'Traveller'}</b><small>{user?.phone||'Profile'}</small></span><em>⌄</em></button>{open&&<div className="profile-pop"><div className="pop-title">TRAVEL PASSPORT</div>{[['Name',user?.name],['Phone',user?.phone],['DOB',user?.dob],['Address',user?.address],['Family',user?.familyName],['Family phone',user?.familyPhone]].map(([a,b])=><div className="pop-row" key={a}><span>{a}</span><b>{b||'—'}</b></div>)}</div>}</div></div></header>}
